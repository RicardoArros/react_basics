import React from "react";

const Recorrer = () => {
  //
  // client_list.map((obj, i) => {
  //   console.log(`${obj.rut} - ${obj.name}`);
  // });

  // blackListData.map((obj, i) => {
  //   console.log(`${obj.rut} - ${obj.name}`);
  // });

  // function esIlegal(ilegal) {
  //   return (
  //     ilegal.name === "Pedro Jimenez" || ilegal.name === "Ximena Rodriguez"
  //   );
  // }

  // const found = client_list.find(esIlegal);

  return <div></div>;
};

export default Recorrer;
